# Entorno real de trabajo: venv_cuda (Python 3.9.13, Windows, RTX 3050 6GB).
# Generado desde el venv que efectivamente funciona, a diferencia de
# requirements.txt, cuyos pins (torch==2.0.1 etc.) no corresponden a nada
# instalado.
#
# Las ruedas de CUDA NO estan en PyPI. Instalar torch primero:
#
#   python -m pip install torch==2.7.1+cu118 torchvision==0.22.1+cu118 \
#       torchaudio==2.7.1+cu118 --index-url https://download.pytorch.org/whl/cu118
#   python -m pip install -r requirements-cuda.txt
#
torch==2.7.1+cu118
torchvision==0.22.1+cu118
torchaudio==2.7.1+cu118

numpy==2.0.2
opencv-python==5.0.0.93
pillow==11.3.0
matplotlib==3.9.4
tqdm==4.70.0

# COCO AP estandar. Trae rueda precompilada para cp39-win_amd64, no hace
# falta compilador de C.
pycocotools==2.0.11

pytest==8.4.2
jupyter
